import { BoldandblueDirective } from './boldandblue.directive';

describe('BoldandblueDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldandblueDirective();
    expect(directive).toBeTruthy();
  });
});
